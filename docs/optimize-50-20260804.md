# 최적화 50선 (2026-08-04 실사)

근거: 빌드 산출물·코드 그렙 실측. 항목마다 [분류·규모] 표기.
진행 표기: ✅ 완료 · 🔄 진행 중 · ⏸ 대기(사유).

## A. 디자인 밀도 (소유자: "구성요소가 아직 약간 큼" — 글씨 유지 원칙)
1. ✅ 모바일 토큰 3차 축소 — pad-compact 12→11 · pad-card 13→12 ·
   pad-hero 19→17 · radius-card 14→13 (md+ 원복 유지, 게이트 짝 검사 통과 필요)
2. ✅ 카드 사이 간격 모바일 축소 — --sp-card-gap 24→20px (md+ 원복 + 게이트 토큰 추가)
3. ✅ 섹션 사이 간격 모바일 축소 — --sp-section 32→28px (md+ 원복 + 게이트 토큰 추가)
4. ✅ 홈 KPI 3장(매매지수·기준금리·대출금리) 모바일 패딩·숫자 1단계 축소
5. ✅ 시장 온도 위젯 행 py 7→6px (모바일 밀도)
6. ✅ btn-secondary radius — 실측 결과 이미 10px(전제 오류), 종결
7. ✅ 홈 히어로(다크 카드) 모바일 여백 p-5→pad-hero(17px)·radius 22→18, md+ 원복
8. 뱃지·칩류(px-2 py-[3px]) 공통 클래스로 수렴 — 크기 편차 제거
9. ✅ 카드 그림자 — 실측 결과 .card 는 이미 플랫(보더만, 호버 시에만 sm), 기충족 종결
10. 모바일 리스트 행(leading·py) 밀도 프리셋 — .row-compact 유틸

## B. 초기 로드 (LCP·JS)
11. ✅ 홈 미니지도 지연 마운트 — IntersectionObserver 로 뷰포트 근접 시에만
    네이버 SDK 로드(홈 첫 페인트에서 외부 SDK 제거)
12. ✅ 목록형 Link prefetch={false} — /tx 지역 그리드·인접 칩(50+ 링크가
    뷰포트 진입마다 RSC 프리페치)
13. ✅ 용어사전 목록 prefetch={false} (56 링크)
14. ✅ 월간 리포트 목록 prefetch={false}
15. ⏸ 홈 피드 prefetch — 첫 화면 링크는 실이동 확률이 높아 프리페치 유지가 체감에 유리하다 판단(보류)
16. ✅ 전체 메뉴 링크 prefetch={false} (9개 렌더 지점 전부)
17. AdSenseLoader — 광고 없는 플랜 판정 전 스크립트 지연(현행 로드 시점 실측)
18. GA4 gtag — 동의 후 idle 로드(requestIdleCallback) 여부 점검
19. framework 청크 188K — next/dynamic 도입 후보 실측(효과 없으면 종결)
20. Pretendard preload 스왑 — 이미 비차단(확인 종결) → 서브셋 여부만 재실측

## C. 이미지
21. ✅ <img> loading="lazy" decoding="async" — 목록·카드 이미지 전수
    (뉴스 목록·라이브러리·매물 패널·노트 미리보기 등)
22. ✅ 뉴스 상세 원문 사진 — 16:9 비율 상자 선점(CLS 제거) + CoverImage lazy(1차)
23. ✅ 사진 업로드 클라 리사이즈 — lib/client/image-resize (긴 변 1600px,
    EXIF 회전 반영, 알파 있으면 PNG 유지, 이득 없으면 원본). 노트·매물 폼 적용.
    실측(375px, 로컬 프로덕션 빌드): 4000×3000 원본 607KB → 업로드 73KB(12%),
    800×600 소형은 재인코딩 없이 통과, 알파 PNG 는 PNG 로 유지.
24. ✅ OG 캐시 헤더 — 프로덕션 실측 `public, immutable, no-transform,
    max-age=31536000` (next/og 기본). 추가 조치 불필요, 종결.
25. ✅ CoverImage — srcSet 이 없어 sizes 는 브라우저가 무시(전제 오류). 변환
    CDN 이 없어 srcSet 소스도 없음. 누락돼 있던 decoding="async" 만 보완하고,
    실제 전송량은 #23 으로 줄인다는 판단을 코드 주석에 남김.

## D. 데이터·캐시
26. ✅ /api/auth/session 수렴 — lib/client/session-lite 공유 프라미스(TTL 30s,
    실패 미캐시). 헤더·전체 메뉴·미니지도·애드센스 유닛·Auto ads 로더·
    개인화 홈·광고 게이트 7곳 전환 — 홈 실측 5회→1회. 결제 직전 확인
    (CheckoutClient 등)은 신선도가 우선이라 직접 조회 유지(의도).
27. 홈 loadNewHomeData 내 중복 호출 실측(react cache 적용 범위)
28. getAllRegionSnapshots 소비처 전수에서 TTL 일관성 점검
29. 어드민 트래픽 페이지 9쿼리 — 뷰 통합 여부 실측(현행 병렬이라 보류 가능)
30. ✅ 홈 공개 노트 조회 50→10행(3건 표시 + 여유분)
31. market_transactions 인덱스 — tx-bands 조회 플랜 실측
32. ✅ PostgREST 상한 미명시 전수 실측 — 81건. 전부 고치는 대신 **잘리면
    숫자가 틀어지는 곳**(합계·건수를 JS 에서 계산)만 우선 처리:
    · /api/admin/stats — 대기 큐 3종을 `count:"exact", head:true` 로 전환
      (행을 받아 .length 로 세던 것 → 정확·무전송), 오늘 결제 합계는 상한
      5,000행 명시 + 과소 집계 시 경고 로그 + 응답에 todayRevenueTruncated
    · lib/admin/stats loadSignupTrend30d — 상한 명시, 닿으면 잘린 그래프 대신 실패
    · lib/points/ledger earnedSince — 상한 명시, 닿으면 던짐(적립 상한 초과 지급 방지)
    · lib/creator/sales — 구매 집계 상한 + 과소 경고
    · lib/watchlist 알림 대상 — 상한 + 도달 시 error 로그(알림 누락 가시화)
    나머지 76건은 개인 단위·소형 테이블 조회로 절단 위험이 낮아 다음 웨이브로.
33. unstable_cache 대상 확대 — 브리핑 외 후보(온도 위젯 이력)
34. /api/metrics/pageview — 삽입 실패 재시도 없음 확인(의도 명시)
35. Supabase 클라이언트 싱글턴 재사용 점검(요청당 생성 여부)

## E. 런타임·상호작용
36. ✅ 전체 메뉴 — backdrop blur 제거·pointerdown 닫기(완료, 0d211d4)
37. 탭바 스크롤 핸들러 — rAF 스로틀 확인(기적용, 종결)
38. NaverMap 마커 재생성 — markers 배열 참조 변경 시 전체 재그리기 여부 실측
39. 검색 인풋 디바운스 실측(글로서리·통합검색 300ms 표준화)
40. rise-in 애니메이션 다발 — prefers-reduced-motion 전수 존중 확인
41. 홈 data-reveal 관찰자 — 단일 IntersectionObserver 공유 확인
42. 콘솔 경고 0 확인(프로덕션 로그 소음)
43. React key 경고 전수(빌드 로그 기준) 확인
44. ✅ 리스너 누수 전수 — addEventListener 40지점 스크립트 대조 결과 React
    컴포넌트는 전부 해제됨(누수 0). 미해제 2건은 layout.tsx 의 `{once:true}`
    (자기 해제)와 문서 수명 인라인 스크립트로, 누수가 아님. 종결.
    다만 그 인라인 스크립트에서 아래 #51 을 발견.
45. 폼 제출 중복 방지(busy 가드) 전수 — 결제·노트 저장 외 잔여 폼

## F. 전송·기타
46. ✅ 정적 자산 — 프로덕션 실측 `public,max-age=31536000,immutable` + CDN HIT. 종결.
47. ✅ no-store 16곳 실측 — 전부 실패(503) 응답·개인/관리자 데이터·POST 라우트로
    캐시 가능한 GET 오용 없음. 종결.
48. ✅ middleware matcher — `_next/static·_next/image·favicon·이미지 확장자` 이미
    제외. 종결.
49. sitemap 생성 비용 실측(빌드 시간 내 비중)
50. ✅ 미사용 의존성 41개 제거 — shadcn 스캐폴딩 잔재(radix 26종·recharts·
    sonner·cmdk·vaul·motion·date-fns·react-hook-form·embla·input-otp·
    react-day-picker·react-resizable-panels·cva·tw-animate-css) + CDN 스크립트로
    쓰는 @tosspayments/payment-sdk, 미사용 @vercel/speed-insights.
    코드 전수 grep 에서 import 0건 확인 후 제거 → 패키지 127개·node_modules
    888MB→774MB(-13%). next.config optimizePackageImports 도 실제 쓰는
    lucide-react 하나만 남김(없는 패키지를 "최적화 중"이라 적어 두지 않는다).

## G. 실사 중 추가 발견
51. ✅ deploy-sync 인라인 스크립트 폴링 3초 → 30초 + 숨은 탭 건너뛰기.
    백그라운드 탭도 3초마다 돌았고, `wd-deploy` 쿠키가 아직 없는 첫 방문에서는
    그 주기마다 /api/health 를 찔렀다(탭 하나 = 시간당 1,200요청).
    복귀 시점은 visibilitychange·pageshow 가 이미 즉시 확인하므로 감지 지연은
    "보고 있는 탭에서 최대 30초"뿐.

## 진행 로그
- 1차(이 커밋): 1~5(디자인 밀도 — 모바일 토큰 3차 축소 + 간격 토큰 2종
  신설·게이트 확장, KPI·온도 위젯 밀도), 11(홈 미니지도 지연 마운트),
  12~14(목록 prefetch 차단), 21(이미지 lazy 전수), 36(기완료 편입).
  375px 실측 후 배포.
- 2차(이 커밋): 7(히어로 모바일 여백), 16(전체 메뉴 prefetch 차단),
  22(뉴스 사진 CLS), 26(세션 호출 4→1 수렴), 30(홈 노트 조회 축소).
  기충족 종결: 6(radius 이미 10px)·9(카드 이미 플랫). 보류: 15(사유 명기).
- 3차(이 커밋): 23(사진 클라 리사이즈 — 실측 12%), 25·24·46·47·48(실측 후 종결),
  32(절단 시 숫자가 틀어지는 조회 5곳 상한·경고·count 전환), 44(누수 0 확인),
  50(미사용 의존성 41개 제거), 51(신규 발견 — deploy-sync 폴링 3s→30s).
