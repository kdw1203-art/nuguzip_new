# 966 — 기능 디테일·디자인 대규모 개편 (2026-09-05)

소유자 요청: "기능적 디테일, 디자인적 개선에 대해 대규모 개편 — 최소 30가지를 한 번에",
"특히 결제 부분에 신경". 아래 34건 전부 이 릴리스 하나에 들어 있다. 번호는 커밋·화면
주석의 `[966]` 태그와 같은 목록이다. 지어낸 숫자·문구 없음(가격은 billing-periods 단일
출처, 문구는 코드가 실제로 하는 일만).

## A. 결제·구독 (16)

| # | 화면 | 바뀐 것 |
|---|---|---|
| P1 | /subscription/checkout | **주문 요약이 결제수단 위젯 위로** — 무엇을 얼마에 사는지 먼저 본다. 금액 옆 "VAT 포함". 로딩·준비 상태에도 "← 돌아가기" 링크 |
| P2 | 결제 버튼 | 비로그인 → 로그인 뒤 **고른 플랜·주기(·returnTo)가 유지**된다(예전엔 /subscription 처음부터) |
| P3 | 플랜 카드(연간) | "오늘 27,600원 결제 · 이후 매년 갱신" — 카드에 실제로 찍히는 총액을 CTA 위에 |
| P4 | 주간권 카드 | 결제 미개통이면 사전 등록(예전엔 버튼이 사라진 죽은 카드) · 프로(expert) 이용자는 구매 차단(사면 플러스로 강등되던 버그) · 플러스 이용자는 "7일 연장" 로 표기 |
| P5 | 오픈 알림 버튼 | 결제 버튼과 같은 바탕 클래스(패딩·라운드 없던 맨 테두리 수정) · "주간권만 구매 가능" 상황을 문구로 |
| P6 | 결제 직후 | **영수증 이메일** 신설(`paymentReceiptEmail`) — 상품·금액(VAT)·일시·이용 종료일·다음 결제 예정·주문번호·매출전표 링크. 토스 단건·빌링 첫 결제·갱신·카카오페이·웹훅 복구 경로 전부 |
| P7 | 결제 직후 | **알림함 알림** "결제가 완료됐어요 · 9월 12일까지 이용" (주문당 1회 — payments.metadata.receiptNotifiedAt 선점) · 환불 시 알림 + 메일(예전엔 무통보 강등) |
| P8 | /payment/success | **"이어서 사용하기"** — 페이월에서 넘어온 경우 원래 화면(returnTo)으로 1차 CTA |
| P9 | 자동결제 등록 | 다음 결제일을 서버와 **같은 식**(만료 2일 전, `nextChargeAtFrom`)으로 — 등록 화면 10/5 vs 성공 화면 10/3 불일치 제거 |
| P10 | 결제 내역 | **모바일 카드형**(6열 가로 스크롤 표 제거) · 상태 칩 · "이전 결제 더 보기"(10→50→100) · 영수증 없으면 "영수증 요청"(이유 없는 — 제거) |
| P11 | 결제 내역 → 고객센터 | 행별 **"환불·문의"** 링크 → /support 가 결제·환불 카테고리·주문번호·금액을 **미리 채운다**(예전엔 딥링크가 무시됨) |
| P12 | /my 구독 카드 | 자동결제면 "다음 결제 10월 3일 · 2,900원/월", 최근 결제 1건 + 결제 내역 링크 |
| P13 | 해지 | 자동결제 이용자에게 겹쳐 보이던 두 해지 버튼 정리 · `window.confirm` → 인라인 2단계(+이탈 사유 칩) · 확인 문구에 **"언제까지 쓸 수 있는지"** 날짜 |
| P14 | 페이월 | **가격 한 줄**(주간권 1,100원부터 · 플러스 월 2,900원 · VAT 포함) + returnTo 로 결제 뒤 복귀 |
| P15 | 만료 알림 | 주간권 구매 다음 날 오던 "7일 뒤 만료" 알림 제거(총 기간 8일 미만은 T-7 건너뜀) · 실패 화면 "다시 시도" 가 결제창 앞까지 직행 · 관리자 결제표 주기 표기 통일(`billingLabel`) |
| P16 | /subscription | **결제 신뢰 스트립** 3칸(카드번호 미저장 · 즉시 적용·영수증 메일 · 7일 청약철회) — 전부 코드가 하는 일 |

## B. 전역 UX (11)

| # | 바뀐 것 |
|---|---|
| G1 | **이탈 방지** `useUnsavedGuard` — 노트 작성(자동저장 사이 구간)·전문가 프로필·매물 등록·동네 글쓰기·설정 SMS 번호에서 저장 안 한 채 닫으면 브라우저가 묻는다 |
| G2 | **맨 위로** 버튼(800px 이상 스크롤 시, 탭바 위, /town·/notes 에서는 FAB 위로 비켜 앉음, 감속 모션 존중) |
| G3 | **오프라인 배너** — 연결 끊기면 하단 한지색 띠, 복구 시 "다시 연결됐어요" 2.5초 |
| G4 | **탭 타겟** `.tap`(주변 8px 확장) — 모든 모달의 ✕, 최근 검색 삭제, 로그인·비밀번호 화면 ✕ |
| G5 | **글자 수 카운터** `<CharCount>` — 견적 요청·고객센터·Q&A 질문/답변·단지 후기(조용히 잘리던 6곳 중 5곳; 동네 글쓰기는 상한이 없어 제외) |
| G6 | **다크 모드**: 설정 › 계정에 시스템/라이트/다크 3단 컨트롤(예전엔 모바일 햄버거에만), `enableSystem` 켜서 OS 설정 선택 가능, 다크에서 흰색으로 남던 표면 정리(덱 뷰어·단지 패널·온보딩) |
| G7 | **검색 드롭다운 키보드** — 헤더 검색이 combobox/listbox(↑↓·Enter·Esc·Home/End) |
| G8 | **인쇄 스타일** — 헤더·탭바·푸터·광고·FAB 숨김, 외부 링크 주소 병기, 여백 14mm (노트·리포트·다이제스트 인쇄) |
| G9 | **공유 버튼 통합** — 5개 구현을 `ShareLinkButton` 하나로(노트·뉴스·모임), 시스템 공유 → 링크 복사 폴백 + 토스트 |
| G10 | **복사 헬퍼** `copyText`/`useCopy` — 12곳 제각각이던 복사를 통일(지도 거리·추천 링크·음성 메모·시나리오 링크) |
| G11 | **저장·팔로우·키워드 알림 피드백** — 조용히 실패하던 세 버튼에 성공/실패 토스트 |

## C. 폼·빈 상태·접근성·성능 (7)

| # | 바뀐 것 |
|---|---|
| G12 | **스켈레톤** — /reports, /imjang (complex·discover 는 페이지가 없거나 리다이렉트라 제외) |
| G13 | **스크롤 위치 복원** — 임장노트·동네 피드·검색에서 상세 → 뒤로 가기 시 보던 자리로 |
| G14 | **외부 링크** `<ExternalLink>`(새 창 안내 + rel=noopener) — 개인정보처리방침의 기관 링크 4곳 |
| G15 | **빈 상태 통일** — /my 의 손으로 짠 빈 블록 4곳 + 관심단지·관심매물·내 매물·리드 화면을 `EmptyState`(아이콘·설명·다음 행동 링크)로 |
| G16 | **드롭다운 접근성** — 계정 메뉴 Esc 닫기·↑↓ 이동·aria-controls, 모바일 메뉴 aria-controls, 헤더 nav 드롭다운 Esc 로 focus-within 해제 |
| G17 | 동네 피드 커버 — 이미 고정 높이로 CLS 를 막고 있어 변경 없음(감사에서 확인만) |
| — | 부수: 모달 헤더 ✕ 최소 32px, 로그인 화면 ✕ 등 |

## 검증
- `npm run build` 전체 게이트 통과(단위검증 195, type-ramp 11=11, dead-controls·route-links·icon-names·contrast 통과), 링크 크롤 195/195, tsc·eslint 0.
- 스크린샷: /subscription 모바일·데스크톱(신뢰 스트립·사전 등록 버튼), 체크아웃, 홈 스크롤(맨 위로 버튼).
- DB 변경 없음(966). 환경변수 추가 없음 — 영수증 메일은 기존 RESEND 설정(EMAIL_FROM)이 있으면 나가고, 없으면 알림함만.
