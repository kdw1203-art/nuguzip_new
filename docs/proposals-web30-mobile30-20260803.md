# 고도화 제안 — 웹 30 · 모바일 30 (2026-08-03 저녁)

전제: enhance-50(8/2)·mobile-30(8/2)은 완료·보류 정리가 끝났다. 이 문서는
**그 이후의 새 관찰**(오늘 실렌더 캡처 + 코드 검토) 기반의 신규 제안이며,
이전 문서의 미완 항목 중 여전히 유효한 것은 [이월] 표시로 포함했다.
원칙 유지: 조회 실패≠데이터 없음 · 근거 없는 항목 없음 · 수치에 기준시점.
난이도 S(≤1h)·M(반나절)·L(하루+), W1(즉시)~W3(후순위).

## 웹 (데스크탑) 30

### A. 첫인상·전환
1. **[W1·S] 쿠키 배너가 화면 중앙 하단을 계속 가림** — 캡처 6장 전부에서
   본문·CTA 위에 떠 있다. 데스크탑은 우하단 코너 카드로 내리고 폭을 줄이면
   가림이 사라진다(모바일은 현행 유지).
2. **[W1·S] 로그인 상태에서 결제 유도 배지 개인화** — /subscription 상단
   "현재 플랜" 배지는 있으나, 홈·헤더에는 플랜 노출이 없다. 헤더 프로필
   영역에 플랜 뱃지(무료/플러스/프로) 1개 — 업그레이드 진입 단축.
3. **[W2·M] 테스트 결제 오픈 공지 배너** — 결제가 방금 열렸는데 알리는
   장치가 없다. 사전 등록(plan_preorder_interest)한 사용자에게 인앱
   알림(받은편지함) 1회 발송 — 등록 데이터가 이미 쌓여 있다.
4. **[W2·M] 홈 데스크탑 우측 사이드바 고정(sticky)** — 긴 좌측 컬럼 스크롤 시
   우측(AI 게이트웨이·시장 온도)이 먼저 끝나 빈 벽이 된다. sticky top 오프셋.
5. **[W2·S] 회원가입 버튼 강조 위계** — 헤더에서 "노트 쓰기"(진한 파랑)와
   "회원가입"(아웃라인)이 경쟁한다. 비로그인 첫 방문의 목표 행동을 하나로
   (실험 프레임워크가 있으니 A/B 후보).

### B. 콘텐츠·SEO
6. **[W1·S] 용어사전 내부 검색 부재** — /glossary 는 56개 용어를 스크롤로만
   찾는다(캡처 확인). 클라이언트 필터 입력 1개면 된다.
7. **[W2·S] 용어사전 → 관련 화면 역링크 확대** — 일부 용어만 href 를 가진다
   (silgeoraega→/tx 등). 시장 온도→/analysis/temperature 등 실존 화면이 있는
   용어 전부에 부여(없는 화면은 만들지 않음).
8. **[W2·M] 월간 리포트에 전월 대비 요약 문장** — /reports/[ym] 상단에
   "전월 대비 거래량 ±N%" 한 줄(두 달치가 있는 달만 — 첫 달은 생략).
9. **[W2·M] 지역 페이지 인접 지역 칩** [이월 39] — 같은 시/도 그룹 지역 칩만
   먼저(좌표 인접 계산은 다음 단계).
10. **[W3·S] llms-full.txt 실데이터 2단계** [이월 40].
11. **[W2·S] 방법론 페이지에 "최근 변경" 섹션** — 방법이 바뀌면 문서를
    갱신한다고 약속했는데 변경 이력이 없다. 마이그레이션·문서 커밋 기준
    수동 CHANGELOG 5줄.
12. **[W3·M] 뉴스 자동수집 출처 표기 강화** — 기사 카드에 출처 파비콘은
    있으나 원문 링크 노출이 약하다. 상세 하단 "원문 보기" 위치·시인성 점검.

### C. 데이터·분석 화면
13. **[W2·M] /tx 데스크탑 지역 그리드 밀도** — 1440px 에서 3열 카드가 성겨
    보인다(캡처). 4열 + 거래량 미니바(정렬 근거 시각화).
14. **[W2·M] 시장 온도 위젯 → 홈 데스크탑에서 스파크라인** — 주간 아카이브에
    지역별 시계열이 이미 있다. 위젯 각 행에 4주 미니 추이.
15. **[W2·M] 단지 비교(/complex/compare) 진입 동선** — 단지 상세에서 "비교에
    추가" 버튼이 있는지 점검, 없으면 헤더 액션에 추가(비교 트레이 기존 로직
    재사용).
16. **[W3·M] 지역 허브에 공급(입주물량) 미니 차트** — UpcomingSupply 데이터가
    표로만 있다. 연도별 막대 1개.
17. **[W3·L] 관심 단지 대시보드(/my/watchlist 승격)** — 지금은 목록만.
    단지별 최근가·변동·새 노트 수를 한 표로(전부 기존 로더 조합).
18. **[W3·M] 웹바이탈 어드민 시각화** [이월 44] — p75 LCP/CLS 주간 추이,
    트래픽 대시보드에 탭 추가(수집은 이미 됨).

### D. 커뮤니티·참여
19. **[W2·M] 글 상세 다음/이전 글 내비게이션** — 뉴스 상세에서 목록으로
    돌아가야만 다음 글을 본다. 같은 카테고리 이전/다음 2링크.
20. **[W2·S] Q&A 답변 유도** — 답변대기 질문을 관련 단지 상세 Q&A 섹션에서
    "답변을 기다리는 질문" 배지로 상단 정렬(이미 렌더되는 데이터의 정렬만).
21. **[W3·M] 신고 처리 결과 통보** [이월 28] — content_reports 상태 변경 시
    신고자 인앱 알림.
22. **[W3·M] 채팅 방별 닉네임** [이월 27] — 가명 대신 방별 닉네임 옵션
    (서버 저장·이메일 비노출 유지, 테이블 1개 추가).

### E. 결제·구독 (오늘 열린 기능의 후속)
23. **[W1·S] 결제 완료 화면에 영수증 링크** — confirm 이 receiptUrl 을
    저장하는데 성공 화면에 안 보여준다. 있으면 노출(없으면 생략).
24. **[W1·S] 마이페이지 결제 내역에 환불 상태 표기** — payments 테이블에
    refunded 가 생겼다. /my 결제 내역이 상태를 구분해 그리는지 점검·보강.
25. **[W2·S] 구독 페이지 비교표와 카드 기능명 동기화 점검** — 카드가
    PLAN_DEFINITIONS 로 옮겨졌는데 하단 FEATURE_ROWS 표는 별도 데이터다.
    두 표의 문구·수치 대조(어긋난 항목은 표를 단일 출처로 수렴).
26. **[W2·M] 사전 등록자 → 결제 전환 추적** — plan_preorder_interest 와
    payments 를 이메일 해시로 대조한 전환율 카드(어드민 수익 페이지).
27. **[W3·M] 자동결제(빌링) 화면 배선** — 토스 추가 계약 승인 후:
    requestBillingAuth → 서버 빌링키 발급(모듈 준비됨) → 갱신 크론.

### F. 운영·품질
28. **[W1·S] 관리자 결제 연동 화면 심사 상태 수동 갱신 칸** — "심사 접수됨
    (2026-08-03)"이 하드코딩이다. 승인되면 낡은 문장이 된다 — 문구를
    "접수일" 사실만 남기고, 승인 후 갱신 체크 항목화.
29. **[W2·S] 배포 후 스모크 배선** [대기] — 패치 보관 중. 토큰 Workflows
    권한만 추가되면 커밋(이미 스크립트는 프로덕션 검증 완료).
30. **[W2·M] 트래픽 대시보드 리텐션 열** — page_view_events 세션키로
    "7일 내 재방문 세션 비율" 1지표(동의 표본 명기 유지).

## 모바일 30

### A. 밀도·타이포 (오늘 2차 축소의 후속)
1. **[W1·S] 축소 후 실기기 검증 라운드** — 시뮬레이터가 아닌 실기기(사장님
   폰) 캡처 3장(홈·단지·구독)으로 13px 패딩·14px 모서리 체감 확인 후
   미세조정(토큰만 조정하면 됨).
2. **[W1·S] 동네이야기 카테고리 카드 축소** — 모바일 캡처에서 카드 1개가
   폭 1/3·높이 260px 로 크다. 아이콘·라벨 2줄 가로형으로 절반 높이.
3. **[W2·S] 글래스 헤더 높이 1단계 축소** — 축소된 본문 대비 헤더(~72px)가
   상대적으로 커졌다. 모바일만 py 1단계 축소(터치 타깃 유지).
4. **[W2·S] 홈 "지도를 준비 중이에요" 자리 절약** — 미로그인 홈의 지도
   placeholder 카드가 화면의 1/4 을 차지한다(캡처). 절반 높이 + 관심지역
   설정 CTA 로 교체.
5. **[W2·S] 탭바 상단 여백 침범 점검** — pb-32 본문 여백과 스크롤 축소
   탭바(translate-y) 조합에서 마지막 요소 가림 여부 재실측.
6. **[W2·S] 쿠키 배너 모바일 위치** — 탭바 바로 위에 겹쳐 두 층이 쌓인다
   (캡처). 동의 전 첫 스크린에서만 노출하고 탭바 여백을 계산에 반영.

### B. 입력·터치
7. **[W1·S] 검색 입력 자동 포커스 옵션** — /search 진입 시 키보드가 안
   올라온다. 탭바 검색 진입은 intent 가 명확 — autoFocus(단, 뒤로가기
   복귀 시엔 미포커스).
8. **[W2·S] 노트 작성 폼 섹션 접기 상태 저장** — 긴 폼에서 스크롤 소모가
   크다. 접기 상태를 draft 에 포함(이미 draft 저장 인프라 있음).
9. **[W2·M] 사진 첨부 진입 1탭 단축** — 현장에서 사진→메모 순서가 많다.
   작성 화면 상단에 카메라 버튼 노출(기존 업로드 로직 재사용).
10. **[W2·S] 별점·만족도 터치 영역 확대** — NoteForm 만족도 입력의 히트
    영역 실측(40px 미만이면 확대).
11. **[W3·M] 뒤로가기 스크롤 복원** [이월 m26] — 목록→상세→뒤로 시 목록
    최상단으로 튄다. scrollRestoration 활성화는 됐는지 실측 후, 무한
    스크롤 목록(피드)에 위치 복원 추가.
12. **[W3·M] 당겨서 새로고침** [이월 m28] — PWA 설치 사용자용, 홈·피드 2곳.

### C. 콘텐츠 소비
13. **[W1·S] 뉴스 상세 본문 행간 모바일 최적화** — leading 1.6→1.75 로
    장문 가독(모바일만).
14. **[W2·S] 면적대별 시세 표 가로 스크롤 힌트** — 단지 상세 표가 4열이라
    좁은 기기(SE)에서 잘림 가능. min-width + 우측 페이드 힌트 실측 적용.
15. **[W2·S] 시장 온도 위젯 행 터치 피드백** — press 효과 없음. active
    상태 추가(기존 .press 유틸 1클래스).
16. **[W2·M] 이미지 srcset** [이월 m25] — 노트·매물 사진 업로드 시 리사이즈
    파이프라인(서버) + srcset. L에 가까운 M — 업로드 경로 1곳씩 순차.
17. **[W2·S] 리포트 표 모바일 카드화 점검** — /reports/[ym] 의 지역 표가
    모바일에서 표 그대로인지 확인, 상위 5행 카드 + "전체 보기" 접기.
18. **[W3·S] 구독 페이지 기능 리스트 접기** — 카드당 기능 7~11줄로 길어졌다
    (오늘 확장의 트레이드오프). 모바일은 상위 5줄 + "전체 기능 보기" 토글.

### D. 현장(임장) 시나리오
19. **[W2·M] 오프라인 노트 임시저장 안내** — 지하주차장 등 무신호에서 작성
    중 이탈 시 draft 가 살아있음을 알리는 오프라인 배너(navigator.onLine).
20. **[W2·S] 현장 모드 진입점 노출 점검** — field session 퍼널 이벤트가
    있는데 진입 버튼이 어디 있는지 실측(죽은 기능이면 노출 or 정리).
21. **[W3·M] 노트 음성 메모** [보류 유지 — 소유자 결정 필요].
22. **[W2·S] 지도 현재 위치 버튼 크기·위치** — 한 손 조작 반경(우하단) 실측,
    탭바와 겹침 여부 확인.

### E. 알림·재방문
23. **[W2·M] 웹푸시 구독 유도 시점 개선** — 설치 프롬프트 15회 노출 실측
    (트래픽 대시보드) 대비 구독 전환 관찰 후, 첫 노트 저장 직후로 시점 이동.
24. **[W2·S] 관심 단지 추가 직후 알림 안내** — watchlist 추가 토스트에
    "시세 변동 시 알림" 사실 명시(price-alerts 크론이 실제로 돈다).
25. **[W3·M] 홈 화면 아이콘 배지(앱 뱃지 API)** — 받은편지함 미읽음 수.
    지원 브라우저 한정, 미지원 시 무동작.

### F. 성능·안정
26. **[W1·S] 폰트 서브셋 실측** [이월 5] — Pretendard dynamic-subset 이
    실제로 서브셋 로드되는지 네트워크 실측(이미 dynamic-subset CSS 사용 중
    — 사실 확인만, 문제 없으면 종결).
27. **[W2·S] 지도 초기 타일 로드 지연 실측** — 모바일 첫 진입 TTI 측정,
    danji 30개 SSR 유지 여부 판단 근거 수집.
28. **[W2·S] 축소 토큰 회귀 가드** — --pad-card 등 모바일 토큰이 md 원복
    블록과 짝이 맞는지 검사하는 린트 1개(check:final-release).
29. **[W2·S] 트래픽 대시보드 모바일 비율 지표** — viewport_group_change
    이벤트(2,867건 실측)로 모바일/데스크탑 세션 비율 카드.
30. **[W3·M] PWA 오프라인 화면 개선** — /offline 폴백이 기본 문구인지 확인,
    최근 본 단지(localStorage)라도 보여주는 정직한 오프라인 화면.

## 권장 실행 순서
- **W1 (바로)**: 웹 1·2·6·23·24·28 + 모바일 1·2·7·13·26 — 전부 S.
- **W2**: 결제 후속(웹 25·26·3)과 모바일 밀도 후속(3·4·6) 우선.
- **W3·보류**: 빌링(토스 계약), 음성 메모(소유자 결정), 신고 통보·닉네임(테이블 추가).

## 진행 로그
- W1 1차(이 커밋): 웹1(쿠키 배너 데스크탑 우하단 코너 360px — 실측 검증),
  웹6(용어사전 검색 — 이름·요약 필터, '전용' 5건 실측), 웹28(어드민 심사
  문구 사실화). 모바일2(카테고리 카드 104×92→92×76), 모바일7(빈 진입 시
  검색 자동 포커스 — ?q= 복귀는 제외), 모바일15(온도 위젯 press).
- 점검 결과 기충족: 웹23(영수증 카드 — 동시 작업으로 구현됨), 웹24(환불
  라벨 존재), 모바일13(뉴스 행간 이미 1.85), 모바일26(dynamic-subset CSS
  사용 확인 — 서브셋 로드 맞음, 종결).
- W2 2차(이 커밋): 웹7(용어사전 6개 항목에 실존 화면 역링크 — /tx·
  /redevelopment·/apply. 전용 화면 없는 용어는 링크 없이 둠 — 죽은 링크
  금지), 웹11(방법론 "최근 변경" 이력 4건 — 해제거래 제외·온도 아카이브·
  집계 복구·면적대 표, 전부 실제 커밋 날짜), 웹25(구독 비교표를
  PLAN_FEATURE_MATRIX 단일 출처로 유도 — 코드에 집행 로직이 없던 광고
  한도(쪽지 일 5건, 사진 50장, 알림 3곳, 시나리오 10개, 플러스 AI 무제한
  등)를 전부 제거하고 실제 한도만 표기. house-ads 문구도 동기화).
  모바일14(면적대 표 min-w 420px + 우측 페이드 스크롤 힌트 — TownCategoryNav
  패턴), 모바일24(관심 단지 저장 성공 시 "시세 변동 시 알림" 한 줄 —
  price-alerts 크론 실재 확인), 모바일28(check-final-release 에 모바일
  토큰↔md+ 원복 짝 검사 게이트 — PASS 36).
- W2 3차(이 커밋): 웹2(헤더 플랜 배지 링크화 — 무료 사용자에게 "무료" 배지
  → /subscription 진입 단축, 유료·관리자 배지도 링크로), 모바일3(헤더
  56→48/48→44px 모바일만, 44px 터치 하한 유지·md+ 원복), 모바일4(지도 폴백
  확정 시 카드 절반 높이 + 관심지역 설정·지도 CTA — NaverMap 에
  onFallbackChange 추가), 모바일6(쿠키 결정 전 탭바 접기 → 하단 한 층,
  배너 bottom 16px). 부수 발견·수정: useCookieConsent 가 같은 문서 안
  인스턴스 간 동기화가 없어 동의 직후 GA4·방문기록·탭바가 리로드 전까지
  결정 전 상태로 남던 버그 — 로컬 이벤트로 즉시 전파(375px 실측: 결정 즉시
  탭바 복원 확인).
- 점검 기충족: 웹8(월간 리포트 리드 문장에 전월 대비 ±N% 이미 포함 —
  두 달치 있는 달만, 첫 달 생략. momText 구현 확인, 종결).
- W2 4차(이 커밋): 웹3(사전등록 오픈 알림 — 어드민 결제 페이지 발송 버튼 +
  /api/admin/payments/notify-preorder. 자동이 아닌 이유: 지금은 테스트
  결제라 "열림" 선언 시점은 운영자가 안다. 멱등 — 발송 기록 이벤트로 중복
  차단, 비로그인 등록은 연락 수단 없음 명시), 웹4(홈 사이드바 lg sticky
  top-76px + self-start), 웹13(/tx 지역 그리드 xl 4열 + 거래량 미니바 —
  최대 지역 대비 비율), 웹14(온도 위젯 4주 스파크라인 — 실측 주간 점수만,
  2주 미만이면 미렌더, lg+ 만), 모바일29(트래픽 대시보드 기기 비율 카드 —
  platform_viewport_group_30d 뷰 신설, initial 세션 시작 그룹만 집계.
  적용 시점 실데이터: 데스크탑 3,058 · 모바일 47 · 태블릿 12 세션/30일).
