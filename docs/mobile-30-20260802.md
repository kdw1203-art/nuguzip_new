# 모바일 개선 30 — 2026-08-02 실측 분석

방법: 프로덕션과 동일 빌드를 iPhone 13 뷰포트(390×844, DPR3)로 렌더해 8개
핵심 화면(홈·노트·동네·실거래·구독·검색·가이드·노트작성)을 캡처하고, 가로
오버플로를 계측(전 페이지 0px — 통과)한 뒤 코드와 대조했다. 각 항목에 근거를
명시한다. 난이도 S/M/L · W1(즉시)~W3.

## A. 전역 레이어 (헤더·탭바·배너)

1. **[W1·S] 쿠키 배너가 반투명이라 밑 텍스트가 비쳐 읽기 어렵다** — 실측
   스크린샷에서 배너 뒤 본문 글자가 배너 문구와 겹쳐 보임(글래스 60% 불투명).
   배너는 동의라는 법적 행위의 UI 다 — 배경을 사실상 불투명(94%)으로.
2. **[W1·S] 쿠키 배너 점유 높이** — 모바일에서 화면 하단 ~1/3(문구 2줄+링크+
   버튼행). 패딩·행간 축소로 1/4 이하로.
3. **[W1·S] 스크롤 시 글래스 헤더 밑으로 본문이 비친다** — home-mid 실측:
   헤더 뒤로 "AI 정리" 제목이 겹쳐 보임(55% 불투명). 70%대로 상향.
4. **[W2·M] 탭바 중앙 기록(+) FAB 가 항상 콘텐츠를 가린다** — 스크롤 중에도
   지름 ~56px 원이 본문 위에 떠 있음(실측). 아래로 스크롤 시 축소/반투명,
   위로 스크롤 시 복원.
5. **[W2·S] 탭바 활성 탭 표시가 점(·) 하나** — 홈 탭 위 파란 점(실측)이
   유일한 상태 표시. 라벨 색 변화가 이미 있으니 점은 유지하되 대비 확인.
6. **[W3·M] 헤더 알림 종 아이콘이 비로그인에도 노출** — 눌러도 로그인 요구.
   비로그인에는 숨기거나 로그인 유도 툴팁.

## B. 터치·입력

7. **[W1·S] iOS 입력 자동 확대(줌) 유발** — 통합검색 입력이 15px(실측,
   search-client.tsx:233). iOS 는 16px 미만 입력 포커스 시 화면을 강제 줌.
   주요 검색·작성 입력을 16px 로.
8. **[W2·M] 칩 터치 타깃 미달** — 필터·태그 칩 다수가 py-1.5(높이 ~30px)로
   44px 권고 미달(뉴스 필터·최근 본 단지 칩 등). 인터랙티브 칩 py 상향 일괄.
9. **[W2·S] 노트 작성 목적 칩 줄바꿈 어긋남** — notesnew 실측: "갈아타기"가
   라벨 열과 어긋나게 단독 줄바꿈. flex-wrap 정렬 보정.
10. **[W3·S] 폼 키보드 타입 최적화** — 가격·면적 입력에 inputmode="numeric"
    부여 여부 전수 점검.

## C. 가로 스크롤 열

11. **[W1·S] 스크롤 힌트 부재** — 동네 카테고리 카드·필터 칩 행이 스크롤
    가능함을 알 방법이 "우연히 잘린 카드"뿐(실측, 스크롤바 숨김). 우측
    페이드(mask) 힌트 추가.
12. **[W1·S] 카테고리 카드 라벨 잘림** — "뉴스·다이제..." 말줄임(실측,
    w-104px 고정). 라벨을 "뉴스"로 줄이고 부제로 이동.
13. **[W2·S] 카드 폭 통일 점검** — 카테고리 카드 104px 는 한 화면 3.5개.
    3개+반(스크롤 암시)이 의도면 유지, 부제 겹침만 해소.

## D. 홈 (모바일)

14. **[W2·S] 히어로 아래 "기록→AI→지도" 칩이 여정 배너와 중복** — 같은 3단계
    가 화면에 두 번(실측). 칩 행 제거 또는 배너 통합.
15. **[W2·M] 모바일 홈 섹션 순서** — 첫 화면에서 CTA 3개(노트 쓰기·지도·AI)
    가 연속 — 위계가 없다. 주 CTA 1 + 보조 2 시각 위계.
16. **[W2·S] 관심지역 카드가 쿠키 배너에 가림** — 첫 방문 실측. 배너 축소(2)
    로 완화되나, 관심지역 카드를 접이식으로 한 단 위로.
17. **[W3·M] 모바일 홈 길이** — mid 캡처 기준 섹션 7+개 세로 나열. 접이식
    섹션(더 알아보기 등) 도입.

## E. 콘텐츠 화면

18. **[W1·S] 실거래 실패/빈 상태 카드의 과대 여백** — /tx 실측: 문구 3줄에
    카드 높이 ~화면 40%(py-8 + 내부 여백). py 축소 — 정직한 문구는 그대로.
19. **[W2·S] 동네 피드 실패 배너와 위치 카드 사이 빈 카드** — town 실측:
    핀 아이콘만 있는 큰 빈 카드가 실패 배너 아래 렌더. 실패 시 지도 카드
    생략 또는 축소.
20. **[W2·M] 뉴스 카드 이미지 비율** — 모바일 1열에서 이미지 세로 비중 점검,
    없는 이미지 자리 회색 박스 크기 축소.
21. **[W2·S] 검색 화면 하부 공백** — search 실측: 추천 지역 아래 화면 절반
    공백. 최근 본 단지 칩(연결 완료)이 채우지만 첫 방문엔 비어 있음 —
    인기 지역/단지 바로가기 추가(실데이터 한정).
22. **[W3·M] 구독 카드 스와이프** — 모바일에서 플랜 3장이 세로 나열(실측
    total 3화면 분량). 가로 스냅 카러셀 + 페이지 점.

## F. 성능·동작

23. **[W2·M] 폴링 절전** — 채팅 5초·지도 재조회 등 폴링이 백그라운드 탭에서
    도는지 점검, visibilitychange 시 중단.
24. **[W2·S] 스켈레톤 모바일 확인** — W1 에서 넣은 4종 스켈레톤의 모바일
    레이아웃 검증(이번 캡처는 정적 상태라 미검증).
25. **[W3·M] 이미지 srcset/sizes** — 노트·뉴스 썸네일 3x 기기에서 원본 크기
    전송 여부 점검 후 next/image sizes 명시.

## G. 제스처·상태 유지

26. **[W2·M] 뒤로가기 스크롤 복원** — 목록→상세→뒤로 시 목록 위치 복원 여부
    전수 점검(Next 기본 + 수동 스크롤 코드 충돌 확인).
27. **[W2·S] 지도 페이지 이탈 확인** — 지도에서 뒤로가기가 필터 상태를 잃는지
    점검(URL 파라미터화 여부).
28. **[W3·L] 당겨서 새로고침** — 피드류(동네·노트)에 pull-to-refresh.

## H. 설치·재방문

29. **[W2·S] 설치 유도(InstallPrompt) 노출 조건 점검** — 재방문 2회+ 등 조건
    이 실제로 동작하는지, 쿠키 배너와 동시 노출 금지.
30. **[W3·M] 모바일 공유 시트** — 단지·노트 공유가 Web Share API 를 쓰는지
    점검(뉴스 상세는 확인됨), 미적용 화면에 일괄 적용.

## W1 (이번 반영): 1·2·3·7·11·12·18 — 전부 S, 시각 회귀 위험 낮음.
