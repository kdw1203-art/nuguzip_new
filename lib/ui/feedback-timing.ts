/**
 * [E79] 화면 피드백의 지속 시간 — 단일 출처.
 *
 * "복사됨" 같은 인라인 확인 표시를 각 화면이 손으로 만들면서 1500·1600·1800·
 * 2000ms 네 가지가 생겼다(실측 6곳). 같은 동작인데 화면마다 다른 속도로
 * 사라지면, 사용자는 그걸 "다른 종류의 알림"으로 읽는다 — 실제로는 우리가
 * 숫자를 따로 적었을 뿐이다.
 *
 * 토스트 프로바이더(app/components/toast/ToastProvider.tsx)의 3초와 성사 연출
 * (MomentProvider)의 1.65초는 성격이 다른 층이라 여기서 건드리지 않는다.
 * 이 상수는 **버튼 자리에서 잠깐 바뀌는 라벨**("복사" → "복사됨")의 시간이다.
 */

/** 버튼 라벨이 "복사됨" 등으로 잠깐 바뀌었다가 되돌아오기까지. */
export const INLINE_CONFIRM_MS = 1_800;
