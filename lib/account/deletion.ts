/** [965] 회원탈퇴 — 화면과 API 가 공유하는 상수 */

/** 실수로 눌러 탈퇴되지 않게 — 화면에서 이 글자를 그대로 입력해야 한다 */
export const DELETE_CONFIRM_WORD = "탈퇴";

/** 접수 뒤 파기까지의 유예(일) — docs/ops/privacy-requests.md 와 같은 값 */
export const DELETE_GRACE_DAYS = 30;
